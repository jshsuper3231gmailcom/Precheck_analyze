</>Markdown

# PreCheck 로그 수집 스케쥴정의서 v1.0

## 문서의 목적
- 로그 수집 시스템에서 참고할 수집 대상서버, 대상서버내 파일 위치/수집 주기를 정의한 파일을 설명
- 로그 수집 시스템내 파일 형태로 정의함

---

## 로그 수집 정의서 파일명
- 로그 수집 시스템 내 ~/cfg/PreCheck_CollectLogs_Schedule.conf

## 로그 수집 주기
- 로그 수집 주기는 다음과 같음
- 배치 -> 일회성 로그 수집, 로그 전체
- 주기 -> 정해진 시간부터 주기별로 로그 수집, 최초 수집은 로그 파일 전체, 이후 주기부터는 추가된 로그만을 수집(마지막으로 읽은 라인번호로 판별)

> **주기 수집 시 파일 읽기 방식 (2026-07-16 개선 — byte offset seek 도입)**
>
> SFTP READ 프로토콜은 원래 임의 byte 위치부터 바로 읽을 수 있음. 이 특성을 활용해 직전 성공 수집이
> `TB_COLLECT_HISTORY.LAST_BYTE_OFFSET`에 남겨둔 위치로 곧바로 seek해서 읽는다 — 이미 읽은 앞부분은
> 더 이상 네트워크로 재전송되지 않는다(과거 버전은 항상 byte 0부터 읽고 클라이언트에서 앞부분을 버렸음).
> `LAST_LINE_NUMBER`는 `TB_COLLECT_LOG.LINE_NUMBER` 등 절대 라인번호 연속성을 위해 오프셋과 별개로
> 계속 병행 유지한다(컬럼 교체가 아니라 추가). 저장된 오프셋이 파일 크기 이상이면(신규 데이터 없음)
> SFTP 접속 자체를 생략한다. `LAST_BYTE_OFFSET`이 아직 없는 과거 이력(컬럼 도입 이전)은 1회에 한해
> 파일 전체를 다시 읽되 `LAST_LINE_NUMBER`까지는 저장하지 않고 건너뛰는 과도기 경로를 거친 뒤 정상
> 경로로 전환된다. 상세 컬럼 정의는 [[4__로그수집_DB정의서]] 참고.
>
> - 로그 파일이 **매일 새 파일로 생성**되는 환경(파일명에 날짜 포함) → 매일 첫 수집만 전체 전송, 이후는 오프셋 재개.
> - **단일 파일에 무한 누적**되는 환경 → 이제 매 주기 신규 증가분만 전송되므로 파일이 커져도 수집 소요 시간이 늘지 않음. 다만 배포 시점에 과도기 경로로 1회 전체 재읽기가 발생하니(위 문단) 대용량 누적 파일은 트래픽 여유 시간대 배포 권장.
> - 검증(2026-07-16, `/tdtest`): 로컬 파이프라인(collect + PostgreSQL 실장비)으로 확인 — 최초 수집 24줄(offset 1873) → 변화 없는 2회는 접속 자체 생략(0건) → 3줄 append 후 재수집 시 정확히 3줄만 처리(offset 2101로 갱신). `TB_COLLECT_LOG` 총 27건, 중복·누락 없음. analyze까지 27건 그대로 반영 확인.

## 로그 수집 정의 파일 포맷
- 로그 수집 정의 파일의 포맷은 다음과 같음
- [서버구분][아이피주소][대상파일명(절대경로)][수집주기기술]
    -> 서버구분 : 대상서버의 호스트명 또는 서버사용명(ex 서버자동주문)
    -> 아이피주소 : 대상서버의 아이피주소
    -> 대상파일명 : 대상서버내 로그 위치, 절대경로
    -> 수집주기기술 : 수집구분, 수집시간, 수집주기

- 수집주기기술은 수집구분별로 다음의 포맷으로 정의
    -> 배치 : [배치|요일|시작시간]
    -> 주기 : [주기|요일|시작시간|수집간격(분)|종료시간]
        -> 요일 표기 규칙
            * 단일 요일 : 0~6 중 하나 (0->일요일, 1->월요일, 2->화요일, ..., 6->토요일)
            * 범위      : 시작-끝 형식 (예: 1-5 = 월~금)
                          - 시작은 끝보다 작거나 같아야 함
                          - 0-6은 *과 동일 (매일)
            * 전체      : * (매일)
            * 콤마(,) 또는 복합 표기(예: 1,3,5 또는 1-3,5)는 1차 버전에서 미지원
            * 위 규칙을 따르지 않는 요일 표기는 스케쥴 무시 처리
        -> 시작시간 : HHmmss, 시작시간은 000001이 최소임
        -> 수집간격(분) : 분단위(예 5:5분)
        -> 종료시간 : HHmmss, 종료시간은 235959가 최대임

- 파일은 텍스트 형태며 개행문자(\n)로 구분
- 위 포맷을 따르지 않는 스케쥴은 무시함
- skip처리는 #으로 시작함
- 수집 실패 시 재시도 정책은 프로그램 명세서를 따름(최초 시도 실패 후 10초 간격 재시도 3회, 배치/주기 공통 적용)

## 스케쥴 예
- #[dlprem01-테스트개발][192.168.210.121][/tmp/check.out][배치|3|090001] -> skip
- [dlprem01-테스트개발][192.168.210.121][/tmp/check.out][배치|3|090001] -> 수요일 9시 00분 01초에 한번 수집
- [dlprem01-테스트개발][192.168.210.121][/tmp/check.out][배치|1-6|090001] -> 월~토요일 9시 00분 01초에 한번 수집
- [dlprem01-테스트개발][192.168.210.121][/tmp/check.out][배치|*|090001] -> 매일 9시 00분 01초에 한번 수집
- [dlprem01-테스트개발][192.168.210.121][/tmp/check.out][주기|*|090001|10|100001] -> 매일 9시 00분 01초부터 10분간격으로 10시 00분 01초까지 수집
- [dlprem01-테스트개발][192.168.210.121][/tmp/check.out][주기|1-5|090001|10|100001] -> 월~금요일 9시 00분 01초부터 10분간격으로 10시 00분 01초까지 수집

---

## 대상파일명 자리표시자 및 날짜 처리 정책

- 대상파일명에 아래 자리표시자를 포함하면 수집 시점에 실제 값으로 자동 치환됨. 자리표시자가 없는 고정 파일명(예: `.log`)은 치환 없이 그대로 사용함.
- 치환 우선순위: `yyyymmdd` → `mmdd` → `$` 순서 (`yyyymmdd`가 `mmdd`를 부분 문자열로 포함하므로 먼저 치환해야 결과가 깨지지 않음).

| 자리표시자 | 치환값 | 예시 |
|---|---|---|
| `yyyymmdd` | 수집일자 전체(yyyyMMdd) | `app.yyyymmdd.log` → `app.20260615.log` |
| `mmdd` | 수집일자의 월/일(MMdd) | `app.mmdd.log` → `app.0615.log` |
| `$` | 수집일자의 요일 숫자(0=일요일~6=토요일, 위 요일 표기 규칙과 동일 기준) | `sys0$.log` → (월요일 수집 시) `sys01.log` |
| (없음, 고정명) | 치환 없음 | `app.log` → `app.log` |

### 배치 처리 시

- 자리표시자 종류와 무관하게 스케줄 시작시간에 단 한 번, 그 시점에 치환된 경로의 파일 전체를 1번 라인부터 끝까지 읽음.
- `LAST_LINE_NUMBER`를 전혀 사용하지 않으므로 "날짜가 바뀌면 리셋"이라는 개념 자체가 없음 — 실행될 때마다 항상 전체를 새로 수집함.

### 주기 처리 시

- 기본 동작: 직전 성공 수집의 `LAST_LINE_NUMBER` 다음 줄부터 파일 끝까지 증분 수집. 단, 직전 성공 수집의 수집일자가 오늘과 다르면 `LAST_LINE_NUMBER`를 무시하고 1번 줄부터 다시 읽음(자정이 지나면 자동 리셋).
- `yyyymmdd` / `mmdd` / `$` 포함: 치환 결과가 매일(또는 매 요일) 다른 경로가 되므로, 날짜·요일이 바뀌면 물리적으로 다른 파일을 보게 됨 — 자정 리셋과 자연히 맞아떨어짐(어제 파일과는 별개 파일이라 처음부터 읽는 게 정상 동작).
  - 다만 `$`(요일)는 0~6 숫자가 7일마다 반복되므로, 같은 요일 파일이 매주 덮어쓰기(rotate)되는 운영 환경을 전제로 함. 덮어쓰기 없이 계속 누적되는 파일이면 지난주 끝 라인부터 잘못 이어 읽힐 수 있음.
- 고정명(`.log`, 자리표시자 없음): 경로가 매일 동일함. 자정이 지나도 같은 파일에 로그가 계속 누적되는 환경이면 경로 끝에 `+`를 붙여 날짜 리셋을 비활성화해야 함(예: `app.log+`). `+`가 없으면 파일이 안 바뀌어도 자정마다 1번 줄부터 재수집됨(=중복 수집처럼 보임).
- `+` 접미사는 `yyyymmdd` / `mmdd`와 함께도 사용 가능(예: `app.yyyymmdd.log+`) — 치환된 당일(또는 당월/일) 파일을 자정 넘어서까지 계속 이어쓰는 드문 경우에 사용.

---

## logdatagen 스킬 연동 현황

- `logdatagen` 스킬이 `precheck_collect\test_dataset\{서버구분}-yyyymmdd.log` 형태로
  생성하는 테스트 로그를 실제 수집 서버가 읽도록, `PreCheck_CollectLogs_Schedule.conf`의
  Dashboard 7개 대상서버(`application.yml`의 `precheck.info-data` 기준) 라인을 해당 경로로
  갱신/활성화함 (2026-06-22 최초 등록, 2026-06-23 경로를 `~/test_log` →
  `precheck_collect\test_dataset`로 변경 — history seed `.sql`이 이미 쓰던 디렉토리와
  통일)
- 대상서버: dlprem01-테스트개발, pamoap01-자동주문, pqgetap1-시세포착1, pqgetap2-시세포착2,
  pjpsap01-주파수클럽, pmaster2-마스터, ddfep01-해외시세 (ddfep01-해외시세는 기존에 등록되어
  있지 않아 신규 추가)
- `dcoodb01-주문체결`은 Dashboard 고정항목 서버가 아니므로 logdatagen 범위 밖, 기존 설정 유지
- logdatagen 스킬이 위 conf를 다시 수정할 경우 이 섹션도 함께 갱신할 것

---

## 서버별 SFTP 포트/계정 override 및 비밀번호 암호화 (2026-07-16 추가)

### 배경
- 기존에는 `application-prod.yml`의 `precheck.sftp.port`/`username`/`password` 전역값 하나를
  모든 대상서버에 동일하게 적용했음.
- 레거시 서버마다 SFTP 포트·계정이 다른 경우를 지원하기 위해, 서버별 override 설정 파일을 추가함.
  기존 `PreCheck_CollectLogs_Schedule.conf` 포맷(4토큰)은 무변경 — 별도 파일로 분리.

### override 정의 파일
- 로그 수집 시스템 내 `~/cfg/PreCheck_CollectServer_Auth.conf` (파일이 없어도 무방 — 없으면
  전역 기본값만 사용)
- 포맷: `[서버구분][포트][계정][비밀번호]`
    -> 서버구분 : `PreCheck_CollectLogs_Schedule.conf`의 서버구분과 반드시 일치해야 매칭됨
    -> 포트     : 비워두면(`[]`) 전역 기본 포트(`precheck.sftp.port`) 사용
    -> 계정     : 비워두면 전역 기본 계정(`precheck.sftp.username`) 사용
    -> 비밀번호 : 아래 "비밀번호 저장 방식" 참고 (ENC(...) 권장)
- override가 필요한 서버만 등록. 이 파일에 없는 서버구분은 전역 기본값을 그대로 사용함.
- `#`으로 시작하는 라인, 빈 줄은 무시.

### 비밀번호 저장 방식
- 비밀번호 필드는 AES-256-GCM으로 암호화한 값을 `ENC(...)`로 감싸 저장함(평문도 동작은 하나
  WARN 로그가 남고, 운영 반입 시 반드시 암호화된 값만 사용).
- 마스터키는 collect 서버의 `COLLECT_AUTH_SECRET_KEY` 환경변수(Base64 인코딩된 32byte 키)에서
  로딩함. conf 파일 자체에는 마스터키가 없고 암호문만 존재.
- 암호화 도구: `collect/scripts/encrypt_password.sh`
  (내부적으로 `com.sks.precheck.collect.tool.CollectAuthEncryptTool` 호출, `collect-*-plain.jar` 필요)
    -> `./encrypt_password.sh --gen-key` : 최초 1회 마스터키 생성
    -> `./encrypt_password.sh "평문비밀번호"` : `ENC(...)` 문자열 생성, conf 파일에 그대로 붙여넣기
- 마스터키 영구 저장: `/home/precheck/cfg/collect_secret.env`에 `COLLECT_AUTH_SECRET_KEY=<값>` 저장
  후 `chmod 600`. collect 기동 스크립트(`collect.sh`/`precheck-common.sh`) 상단에서 `source`하거나
  systemd면 `EnvironmentFile=`로 자동 로드. `encrypt_password.sh`도 현재 쉘에 값이 없으면 이 파일을
  자동으로 읽어옴(우선순위: 현재 쉘 env > secret 파일).

### 신규 서버 추가 절차
1. 신규 서버의 접속정보(포트/계정/비밀번호) 확인
2. 마스터키는 이미 등록된 것을 그대로 사용(재발급 불필요) — `./encrypt_password.sh "신규비밀번호"`로
   `ENC(...)` 생성
3. `PreCheck_CollectServer_Auth.conf`에 `[서버구분][포트][계정][ENC(...)]` 한 줄 추가
4. collect 앱 재시작 (override는 기동 시 1회만 로딩 — 무중단 반영 안 됨)

### 예시
```
[pamoap01-자동주문][2201][pamoap01_user][ENC(...)]
[pqgetap2-시세포착2][][][ENC(...)]   # 포트/계정은 전역값 그대로, 비밀번호만 override
```

---

## 운영 배포 전 SFTP 연결 확인 체크리스트

수집 서버를 운영 환경에 처음 투입하기 전, 아래 순서로 대상 서버와의 SFTP 연결 가능 여부를 확인한다.
모든 명령은 **수집 서버(Linux)** 에서 실행한다.

### 1단계 — 네트워크 도달 가능성

```bash
ping -c 3 <대상서버IP>
nc -zv <대상서버IP> <포트>   # 포트: PreCheck_CollectServer_Auth.conf에 override 있으면 그 값, 없으면 전역 기본(보통 22)
```

- `nc` 결과에 `open`이 출력되면 SSH 포트 도달 가능.
- 실패 시 방화벽/라우팅 확인. 이 단계 실패 시 이하 테스트 의미 없음.

### 2단계 — SSH 비밀번호 로그인

```bash
ssh -o StrictHostKeyChecking=no <username>@<대상서버IP>
```

수집 서버는 `PromiscuousVerifier`(호스트키 검증 생략)로 동작하므로 `-o StrictHostKeyChecking=no`가 동일 조건임.

| 오류 메시지 | 원인 및 조치 |
|---|---|
| `Permission denied (publickey)` | 대상 서버 `/etc/ssh/sshd_config`에 `PasswordAuthentication yes` 추가 필요 |
| `Connection refused` | sshd 미실행 — 대상 서버 SSH 데몬 기동 확인 |
| `Connection timed out` | 방화벽 차단 — 1단계 재확인 |

### 3단계 — SFTP 접속 및 파일 접근

```bash
sftp -o StrictHostKeyChecking=no <username>@<대상서버IP>

sftp> ls /path/to/logfile      # 파일 존재 확인
sftp> stat /path/to/logfile    # 파일 크기·권한 확인 (getFileSizeBytes 동작 검증)
sftp> get /path/to/logfile /tmp/   # 실제 다운로드 (readLines 동작 검증)
sftp> exit
```

conf 파일 경로에 `yyyymmdd` 자리표시자가 포함된 경우, 오늘 날짜로 치환한 실제 경로로 테스트한다.

### 4단계 — 비대화형 one-liner (스크립트 자동화)

```bash
# stat 확인
sftp -o StrictHostKeyChecking=no -o BatchMode=no <username>@<대상서버IP> <<EOF
stat /path/to/logfile
exit
EOF

# 파일 앞부분 확인 (인코딩·포맷 육안 검증)
ssh -o StrictHostKeyChecking=no <username>@<대상서버IP> "head -20 /path/to/logfile"
```

### 5단계 — 파일 인코딩 확인

```bash
ssh <username>@<대상서버IP> "file /path/to/logfile"
ssh <username>@<대상서버IP> "head -5 /path/to/logfile | cat -v"
```

- 레거시 C 기반 서버는 **EUC-KR** 인코딩인 경우가 많음.
- EUC-KR 확인 시 `application-prod.yml`의 `precheck.collect.file-charset: EUC-KR` 설정 필요.
- 기본값은 UTF-8이며 로컬/테스트 프로파일은 UTF-8 고정.

### 체크리스트 요약

| 항목 | 명령 | 성공 기준 |
|---|---|---|
| 포트 도달 | `nc -zv IP 22` | `open` 출력 |
| SSH 로그인 | `ssh user@IP` | 셸 진입 |
| SFTP stat | `sftp` → `stat /path` | 파일 크기 출력 |
| 파일 다운로드 | `sftp` → `get /path` | 로컬 저장 성공 |
| 파일 권한 | stat 결과 | 계정이 read 가능 |
| 인코딩 | `file /path` | UTF-8 또는 EUC-KR 확인 후 yml 반영 |